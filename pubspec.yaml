name: ict_journal
description: ICT Liquidity Sweep Trade Journal

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2   # persist trades + checklist as JSON
  intl: ^0.19.0                 # date formatting
  uuid: ^4.3.3                  # unique trade IDs

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
